spring:
  application:
    name: HYDROLINK_STG
  datasource:
    url: jdbc:mysql://localhost:3306/hydrolink?useSSL=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: true

jwt:
  secret:
    key: exampleSecretKey
  user:
    generator: exampleUserGenerator

frontend:
  url: http://localhost:4200